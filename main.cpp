#include <iostream>
#include "fix-sshadowba!.cop" // ty bleek
#include "enet.socks5.h" // ty bleek
#include "sys-freeezer.hpp" // system freezer thanks idea for zenixa !
#include "dupe_chit.h"

using namespace std;

int MemAddress(int MA1, int MA2)
{
    MA1 = 7999; // dup giltch
    MA2 = 8432; // cesino roullete heck
    MA3 = 4832; // cuztom choos roullete number hax
    MA4 = 3828; // use shadow ban fix ! ty bleek
    fixer_shalfowban(); // make.work
    MA5 = 4444; // infinite bot generator ty bleek
    addsocks5Hack(); // must do to do bot geneerteorr                                   b$
    MA6 = 6942; // freezs system
    sys_freezer(); // must do to work!
}

int main()
{
    int MA1, MA2, MA3, MA4, MA5, MA6;
    
    MemAddress(MA1, MA2, MA3, MA4, MA5, MA6);
    do_dupe(200, 8); // 200 bedrocks omg men
    cout << &MA1 << endl;
    cout << &MA2 << endl;
    cout << &MA3 << endl;
    cout << &MA4 << endl;
    cout << &MA5 << endl;
    cout << &MA6 << endl;
    
    while (true)
        int* dupeglitch = new int(INT_MAX);
}
